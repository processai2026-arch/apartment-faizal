import NotificationCenter from '@/components/features/NotificationCenter';

export default function SecurityNotifications() {
  return (
    <NotificationCenter
      role="security"
      title="Security Notifications"
      description="Watch visitor movement alerts, maintenance reminders, emergency broadcasts, and gate-related updates."
    />
  );
}
