# Database

`officegate_production.sql` is the production MySQL schema for the PHP backend.

The backend also contains SQLite migrations in `backend/database/migrations/` for local development. Use this root `database/` folder when deploying to MySQL/MariaDB.
